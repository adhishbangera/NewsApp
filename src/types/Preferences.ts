// types/Preferences.ts
export interface Preferences {
    categories: string[]; // List of preferred categories (e.g., "Sports", "Technology")
    sources: string[]; // List of preferred news sources (e.g., "BBC", "CNN")
    authors: string[]; // List of preferred authors
  }
  